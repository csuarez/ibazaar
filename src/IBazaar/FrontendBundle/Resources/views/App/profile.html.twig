{% extends 'IBazaarFrontendBundle::base.html.twig' %}

{% block content %}
	<div class="row">
		<div class="col-lg-12">
			<h1>{{ currentApp.name }}</h1>
		</div>
		<div class="col-lg-3 text-center">
			<p><img src="{{ currentApp.logoUrl }}" alt="" width="250" height="250"></p>
			<a class="btn btn-success" href="{{ currentApp.marketUrl }}">
				{% if currentApp.isFree %}
					DESCARGAR
				{% else %}
				    COMPRAR
				{% endif %}
			</a>
		</div>
		<div class="col-lg-9">
			<dl class="dl-horizontal">
				<ul class="list-group">
					<li class="list-group-item">
						<dt>Precio</dt>
						<dd>{{ currentApp.price }} €</dd>
					</li>
					<li class="list-group-item">
						<dt>Descargas</dt>
						<dd>{{ currentApp.downloads }}</dd>
					</li>
					<li class="list-group-item">
						<dt>Fecha de alta</dt>
						<dd>{{ currentApp.createdAt | date}}</dd>
					</li>
					<li class="list-group-item">
						<dt>Descripción</dt>
						<dd>{{ currentApp.description }}</dd>
					</li>
				</ul>
			</dl>
		</div>
	</div>
{% endblock %}